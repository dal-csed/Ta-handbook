import React from "react";
import CustomHeader from "../../../components/CustomHeader";
import CustomFooter from "../../../components/CustomFooter";

const page = () => {
  return (
    <div>
      <CustomHeader />
      This page will contain information and resources about using MS Teams
      as a TA.
      <CustomFooter />
    </div>
  );
};

export default page;
